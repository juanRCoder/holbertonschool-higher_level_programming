# Pyhton - Inheritance in classes

- In this project, we'll go over inheritances in class:

```py
   # Base class
   class human:
        def speak(self):
            print("Hi gyus!")


    # child class
    class juan(human):
        def speak(self):
            print("Hola Juan")
```
