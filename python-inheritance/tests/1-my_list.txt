The ``1-my_list`` module
============================

Using ``MyList``
---------------------

Importing function from the module:
    >>> MyList = __import__('1-my_list').MyList


Printing a sorted list:
    >>> my_list = MyList()
    >>> my_list.append(1)
    >>> my_list.append(4)
    >>> my_list.append(2)
    >>> my_list.append(3)
    >>> my_list.append(5)
    >>> my_list.print_sorted()
    [1, 2, 3, 4, 5]

Printing a sorted list(repeated numbers):
    >>> my_list = MyList()
    >>> my_list.append(1)
    >>> my_list += [1]
    >>> my_list.append(2)
    >>> my_list.append(2)
    >>> my_list.print_sorted()
    [1, 1, 2, 2]

Printing an sorted list 3 (one number)
    >>> my_list = MyList()
    >>> my_list += [3]
    >>> my_list.print_sorted()
    [3]

Printing an empty list
    >>> my_list = MyList()
    >>> my_list.print_sorted()
    []

Printing an sorted list(positive and negative numbers)
    >>> my_list = MyList()
    >>> my_list += [5]
    >>> my_list.append(-4)
    >>> my_list += [-1]
    >>> my_list.print_sorted()
    [-4, -1, 5]

Printing an empty list
    >>> my_list = MyList()
    >>> my_list.print_sorted()
    []

MyList is an instance of the class list
    >>> my_list = MyList()
    >>> isinstance(my_list, list)
    True

Test case check printable string.
    >>> my_list = MyList()
    >>> my_list.append(1)
    >>> print(my_list)
    [1]
